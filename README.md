# Stock Website

Ready-to-deploy React app for a simple stock platform.

## Notes
- Uses Finnhub demo API token in StockDetails (`/stock/:symbol`).
- To deploy on Vercel, connect your GitHub repo and push this project. Vercel will build using `npm run build`.

## Available scripts
- `npm start` — run locally
- `npm run build` — production build
